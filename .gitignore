node_modules/
dist/
*.js.map

# Private keys — NEVER commit
deploy_key
deploy_key.pub

# Runtime data — stay local
.env
logs/
*.log

# Media files — keep directory structure, ignore content
media/*
!media/.gitkeep
